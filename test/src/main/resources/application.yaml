spring:
  name: server-template
  datasource:
    url: jdbc:mysql://localhost:3306/testfinale?allowPublicKeyRetrieval=true&useSSL=false
    username: root
    password: bnck028360
  jpa:
    database-platform: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: update #none
    show-sql: true


